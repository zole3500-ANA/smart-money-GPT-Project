
# Version 2.9 Notes

UI cleanup:
- Removed the `Raw JSON` section from the Streamlit dashboard.
- Kept all analysis tables, chart, component scores, confidence verification, and beginner-friendly interpretations.
- JSON remains available internally in the report object but is no longer shown to end users by default.
